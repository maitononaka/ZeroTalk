{
  "name": "zerochat-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute zerochat_db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.39.0"
  },
  "version": "1.0.0"
}
